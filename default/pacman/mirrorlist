# Default mirrorlist; regenerated via reflector during install (install/preflight/pacman.sh)
# and on update. geo.mirror.pkgbuild.com is Arch's official geoIP CDN mirror.
Server = https://geo.mirror.pkgbuild.com/$repo/os/$arch
