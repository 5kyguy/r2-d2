#!/bin/bash

# Overwrite the user-facing Walker configs and restart the services.
# Repo-managed support assets for Walker are installed separately.

# Ensure walker is set to autostart
mkdir -p ~/.config/autostart/
cp ~/.config/walker/walker.desktop ~/.config/autostart/

# And restarts if it crashes or is killed
mkdir -p ~/.config/systemd/user/app-walker@autostart.service.d/
cp ~/.config/walker/restart.conf ~/.config/systemd/user/app-walker@autostart.service.d/restart.conf

systemctl --user daemon-reload

# Refresh configs
r2-d2-refresh-config walker/config.toml

r2-d2-refresh-config elephant/calc.toml
r2-d2-refresh-config elephant/desktopapplications.toml

# Restart service
r2-d2-restart-walker
